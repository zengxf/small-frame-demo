# 问题

## 企业版 CP 测试没成功

- 可能是依赖有问题，同时不要额外添加 `hazelcast` 依赖
- 删除 `hazelcast` 依赖，IDEA 编译有问题时，加个 maven 源，同时检查下 `hazelcast-enterprise` jar 包

## SQL 测试依赖问题

- 添加 `hazelcast-sql` 依赖后，EmbeddedServer1/2/3 都要重启下